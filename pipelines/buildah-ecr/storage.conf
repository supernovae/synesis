# Buildah storage config. overlay is default (fast); vfs for restricted envs.
# With privileged SCC, overlay works. Set BUILDAH_DRIVER=vfs to force vfs if needed.

[storage]
driver = "overlay"

[storage.options]
additionalimagestores = []
