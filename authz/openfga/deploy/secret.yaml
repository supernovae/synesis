apiVersion: v1
kind: Secret
metadata:
  name: openfga-config
  namespace: synesis-authz
  labels:
    app.kubernetes.io/name: openfga
    app.kubernetes.io/part-of: synesis
type: Opaque
# Intentionally empty. Populate this Secret from your environment-specific
# secret manager or overlay; applying placeholder datastore credentials or
# preshared keys would expose the authorization service.
stringData: {}
