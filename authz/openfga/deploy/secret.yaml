apiVersion: v1
kind: Secret
metadata:
  name: openfga-config
  namespace: synesis-authz
  labels:
    app.kubernetes.io/name: openfga
    app.kubernetes.io/part-of: synesis
type: Opaque
stringData:
  OPENFGA_DATASTORE_URI: "postgres://openfga:changeme@openfga-postgres:5432/openfga?sslmode=disable"
  OPENFGA_AUTHN_PRESHARED_KEYS: "changeme-generate-a-real-key"
