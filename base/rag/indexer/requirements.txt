httpx>=0.27.0
boto3>=1.35.0
# synesis_telemetry: installed separately in Dockerfile (UBI base-api path copy, not base-api image)
defusedxml>=0.7.0
pymilvus>=2.4.0
pyyaml>=6.0
# PDF parsing
PyMuPDF>=1.24.0
# HTML parsing and content extraction
beautifulsoup4>=4.12.0
trafilatura>=2.0.0
# Web crawling — pin exact version; pulls nltk transitively (pip-audit ignores
# for nltk CVEs are indexer-only — see .github/SECURITY.md).  Also pulls
# unclecode-litellm (a litellm fork) which is unused by indexer code — stripped
# post-install in the Dockerfile.  See docs/LITELLM.md § Supply-Chain Security.
crawl4ai==0.8.6
# Code parsing (tree-sitter — 170+ languages via language-pack)
tree-sitter>=0.25.2
tree-sitter-language-pack>=0.13.0
cryptography>=46.0.7
requests>=2.33.0
pygments>=2.20.0
