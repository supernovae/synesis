You are enriching a SynPack v2 platform CLI/operator workflow chunk.

Source chunk:
{{PLATFORM_DOC_OR_SPEC_CHUNK}}

Return one JSON object. Capture exact commands, flags, prerequisites, expected output, failure modes, verification loops, and safe remediation steps. Include kubectl, oc, helm, kustomize, argocd, kubeconform, conftest, and operator tooling when evidenced by the source.

Include SynPack v2 fields when evidenced: task_intents, query_aliases, agent_query_hints, api_contract, version_scope, performance_notes, canonical_examples, anti_patterns, hidden_warnings, verification_hints, related_interfaces, related_symbols, agent_actions, evidence_spans, what_to_use, and minimal_example.
