You are enriching a SynPack v2 platform manifest safety chunk.

Source chunk:
{{PLATFORM_DOC_OR_SPEC_CHUNK}}

Return one JSON object. Focus on secure defaults, admission behavior, RBAC, network exposure, storage safety, rollout safety, policy checks, and commands a developer should run before applying manifests. Prefer concrete field paths and command lines over generic advice.

Include SynPack v2 fields when evidenced: task_intents, query_aliases, agent_query_hints, api_contract, version_scope, performance_notes, canonical_examples, anti_patterns, hidden_warnings, verification_hints, related_interfaces, related_symbols, agent_actions, evidence_spans, what_to_use, and minimal_example.
