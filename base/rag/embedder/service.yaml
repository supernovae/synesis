apiVersion: v1
kind: Service
metadata:
  name: embedder
  namespace: synesis-rag
  labels:
    app.kubernetes.io/name: embedder
    app.kubernetes.io/part-of: synesis
    app.kubernetes.io/component: rag
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: embedder
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
