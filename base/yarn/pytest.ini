[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    integration: live HTTP tests (SYNESIS_TEST_AUTH, SYNESIS_YARN_BASE_URL; optional SYNESIS_LIVE_CHAT=1)
    asyncio: async tests
