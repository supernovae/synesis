apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: synesis-webui

resources:
  - namespace.yaml
  # webui-api-key: managed only by deploy.sh — applying sk-synesis-change-me
  # from git on every deploy overwrote the real key synced from litellm-secrets.
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  - network-policy.yaml
  - secret-copy-job.yaml
