complexity_weights:
  ml_training:
    weight: 12
    keywords: [training, fine-tune, model]

domain_keywords:
  ml_ops:
    domain: ml_ops
    min_hits: 2
    keywords: [pytorch, tensorflow, mlflow]

vertical_prompt:
  name: llm_rag
  active_domain_refs: [ml_ops, cloud]
  platform_context_aliases: [synesis]
  worker_persona_block: |
    You are an ML/RAG specialist. Ground answers in retrieval evidence.
  planner_decomposition_rules: |
    Decompose into data-pipeline, model-serving, and evaluation steps.
  critic_mode: tiered
  critic_tiers:
    basic: "Check factual accuracy of claims."
    advanced: "Verify retrieval grounding and model architecture choices."
    research: "Assess novelty, reproducibility, and experimental rigor."
