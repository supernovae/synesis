apiVersion: v1
kind: Service
metadata:
  name: synesis-admin-mcp-ts
  namespace: synesis-admin
  labels:
    app: synesis-admin-mcp-ts
spec:
  selector:
    app: synesis-admin-mcp-ts
  ports:
    - name: http
      port: 8102
      targetPort: 8102
      protocol: TCP
  type: ClusterIP
