apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: synesis-mcp
  namespace: synesis-yarn
  labels:
    app: synesis-mcp
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: synesis-mcp
