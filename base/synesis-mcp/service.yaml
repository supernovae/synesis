apiVersion: v1
kind: Service
metadata:
  name: synesis-mcp
  namespace: synesis-yarn
  labels:
    app: synesis-mcp
spec:
  selector:
    app: synesis-mcp
  ports:
    - port: 8100
      targetPort: 8100
      protocol: TCP
  type: ClusterIP
