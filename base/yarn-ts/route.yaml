apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: synesis-yarn-ts
  labels:
    app.kubernetes.io/name: synesis-yarn-ts
    app.kubernetes.io/component: yarn
spec:
  to:
    kind: Service
    name: synesis-yarn-ts
  port:
    targetPort: http
  tls:
    termination: edge
