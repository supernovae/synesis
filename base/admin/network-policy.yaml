apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: admin-network-policy
  namespace: synesis-admin
  labels:
    app.kubernetes.io/part-of: synesis
    app.kubernetes.io/component: admin
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: synesis-admin
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Accept traffic from the OpenShift router (or any ingress controller)
    - ports:
        - port: 8080
          protocol: TCP
  egress:
    # Postgres (CloudNativePG in same namespace)
    - to:
        - podSelector:
            matchLabels:
              cnpg.io/cluster: synesis-admin-db
      ports:
        - port: 5432
          protocol: TCP
    # Planner (synesis-planner namespace) — traces, assistant, pipeline test
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: synesis-planner
      ports:
        - port: 8000
          protocol: TCP
        - port: 8080
          protocol: TCP
    # NornicDB (synesis-rag namespace) — content graph stats, knowledge gap validation
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: synesis-rag
      ports:
        - port: 19530
          protocol: TCP
    # Keycloak JWKS endpoint (synesis-auth namespace)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: synesis-auth
      ports:
        - port: 8080
          protocol: TCP
        - port: 8443
          protocol: TCP
    # DNS — any destination (see planner network-policy.yaml)
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
