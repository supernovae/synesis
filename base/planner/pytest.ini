[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    online: test requires a running planner, embedder, or LLM service
    container: test requires container-only deps (langgraph, fastapi, etc.)
    asyncio: mark test as async (pytest-asyncio)
