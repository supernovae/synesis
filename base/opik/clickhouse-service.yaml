apiVersion: v1
kind: Service
metadata:
  name: clickhouse
  namespace: synesis-opik
  labels:
    app.kubernetes.io/name: clickhouse
    app.kubernetes.io/part-of: synesis
    app.kubernetes.io/component: opik-storage
spec:
  selector:
    app.kubernetes.io/name: clickhouse
  ports:
    - name: http
      port: 8123
      targetPort: http
    - name: native
      port: 9000
      targetPort: native
