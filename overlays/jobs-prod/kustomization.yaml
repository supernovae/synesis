apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Production indexer: weekly schedule (base default).
# Usage: ./scripts/deploy-indexer.sh

resources:
  - ../../base/rag/indexer

images:
  - name: synesis-indexer
    newName: ghcr.io/supernovae/synesis/indexer
    newTag: latest
